package Lessons.Task4.Task1;

import org.junit.jupiter.api.Test;

public class QuadraticEquationProcessorTest {
    @Test
    void test(){
        /*Assertions.assertThrows(ArithmeticException.class,() -> {QuadraticEquationProcessor q = new QuadraticEquationProcessor(new QuadraticEquation(1,2,3));q.getBiggerRoot();});
        Assertions.assertThrows(ArithmeticException.class,() -> {QuadraticEquationProcessor q = new QuadraticEquationProcessor(new QuadraticEquation(0,0,3));q.getBiggerRoot();});
        QuadraticEquationProcessor q = new QuadraticEquationProcessor(new QuadraticEquation(2,0,4));
        Assertions.assertEquals(q.getBiggerRoot(),2d,1e-10);*/
    }
}
